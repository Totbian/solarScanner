package com.example.solarscanner;

import android.graphics.Color;
import android.graphics.drawable.Drawable;
import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;
import android.view.KeyEvent;
import android.view.MotionEvent;
import android.view.View;
import android.widget.Button;

public class Main2Activity extends AppCompatActivity {

    Button btn_ferde,btn_lapos;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main2);

//On our create method  , call our buttons ID on XML as defined below




//TouchListener listens for an event, if user performs an action, either on touch or on release as shown below
//Change the button background color from red to blue on touch and  blue to red on release

    }

}
